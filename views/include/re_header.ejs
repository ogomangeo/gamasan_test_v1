<!-- views/include/re_header.ejs -->
<nav class="navbar">
    <div class="nav-brand">
        <a href="/home">Gamasan</a>
    </div>
    
    <!-- 모바일 메뉴 토글 버튼 -->
    <button class="nav-toggle" aria-label="toggle navigation">
        <span class="hamburger"></span>
    </button>

    <div class="nav-menu">
        <ul class="nav-list">
            <li><a href="/program" class="nav-link <%= (locals.path?.startsWith('/program') || locals.title === 'Program') ? 'active' : '' %>">프로그램즈</a></li>
            <li><a href="/mart" class="nav-link <%= (locals.path?.startsWith('/mart') || locals.title === 'Mart') ? 'active' : '' %>">가마산마트</a></li>
            <li><a href="/archive" class="nav-link <%= (locals.path?.startsWith('/archive') || locals.title === 'Archive') ? 'active' : '' %>">아카이빙</a></li>
            <li><a href="/event" class="nav-link <%= (locals.path?.startsWith('/event') || locals.title === 'Event') ? 'active' : '' %>">이벤트</a></li>
            <li><a href="/blog" class="nav-link <%= (locals.path?.startsWith('/blog') || locals.title === 'Blog') ? 'active' : '' %>">블로깅</a></li>
            <li><a href="/notice" class="nav-link <%= (locals.path?.startsWith('/notice') || locals.title === 'Notice') ? 'active' : '' %>">공지사항</a></li>
            <li><a href="/about" class="nav-link <%= (locals.path?.startsWith('/about') || locals.title === 'About') ? 'active' : '' %>">ABOUT</a></li>
        </ul>
    </div>

    <div class="nav-ad">
        <a href="#">
            <img src="/img/sidebar-banner-2.png" alt="광고 배너" class="nav-ad-image">
        </a>
    </div>
</nav>

<style>
/* 상단 여백을 위한 빈 공간 */
body {
    padding-top: 120px; /* 상단 헤더(_header.ejs)와 네비게이션 바의 높이를 고려한 여백 */
}

.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: white;
    padding: 1rem;
    box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    position: fixed;
    top: 40px; /* 상단 헤더의 높이만큼 아래로 이동 */
    left: 0;
    right: 0;
    z-index: 999; /* _header.ejs보다 낮은 z-index 설정 */
}

/* _header.ejs를 위한 스타일 */
.header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    background-color: white;
    z-index: 1000; /* navbar보다 높은 z-index */
}

.nav-brand a {
    font-size: 1.5rem;
    font-weight: bold;
    text-decoration: none;
    color: #333;
}

.nav-menu {
    display: flex;
    align-items: center;
}

.nav-list {
    display: flex;
    list-style: none;
    margin: 0;
    padding: 0;
    gap: 2rem;
}

.nav-link {
    text-decoration: none;
    color: #333;
    font-weight: 500;
    padding: 0.5rem;
    transition: color 0.3s ease;
}

.nav-link:hover {
    color: #007bff;
}

.nav-link.active {
    color: #007bff;
    border-bottom: 2px solid #007bff;
}

.nav-ad {
    max-width: 200px;
}

.nav-ad-image {
    max-height: 40px;
    width: auto;
}

.nav-toggle {
    display: none;
}

/* 반응형 스타일 */
@media (max-width: 968px) {
    body {
        padding-top: 100px; /* 모바일에서는 여백을 줄임 */
    }

    .nav-toggle {
        display: block;
        padding: 0.5rem;
        background: none;
        border: none;
        cursor: pointer;
    }

    .hamburger {
        display: block;
        position: relative;
        width: 24px;
        height: 2px;
        background: #333;
    }

    .hamburger::before,
    .hamburger::after {
        content: '';
        position: absolute;
        width: 100%;
        height: 2px;
        background: #333;
        transition: all 0.3s ease;
    }

    .hamburger::before { top: -6px; }
    .hamburger::after { bottom: -6px; }

    .nav-menu {
        display: none;
        position: absolute;
        top: 100%;
        left: 0;
        right: 0;
        background: white;
        padding: 1rem;
        box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }

    .nav-menu.show {
        display: block;
    }

    .nav-list {
        flex-direction: column;
        gap: 1rem;
    }

    .nav-ad {
        display: none;
    }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', () => {
    const navToggle = document.querySelector('.nav-toggle');
    const navMenu = document.querySelector('.nav-menu');
    
    navToggle.addEventListener('click', () => {
        navMenu.classList.toggle('show');
    });
});
</script>